# Archived mechanics tests

These tests are pre-v2 mechanics-oriented assertions preserved for reference.

They validate deterministic scoring internals, lexical marker extraction behavior,
and score contribution plumbing that was superseded by the v2 routing path based on
`TaskAnalyzer.propose_route()`.

They are intentionally excluded from the active suite and retained in this folder
for historical context and regression archaeology.
